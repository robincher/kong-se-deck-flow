_format_version: "3.0"
_info:
  defaults: {}
  select_tags:
  - se-deck-flow
services:
- connect_timeout: 60000
  enabled: true
  host: httpbin.org
  name: Httpbin-Demo-Service
  port: 443
  protocol: https
  read_timeout: 60000
  retries: 5
  tags:
  - se-deck-flow
  write_timeout: 60000
