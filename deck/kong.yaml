_format_version: "3.0"
_info:
  select_tags:
  - se-deck-flow
services:
- connect_timeout: 60000
  host: httpbin.org
  name: Httpbin-Robin-Service
  port: 443
  protocol: https
  read_timeout: 50000
  retries: 5
  tags:
  - se-deck-flow
  write_timeout: 60000
